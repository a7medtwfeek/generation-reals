# دليل رفع المشروع على GitHub
# GitHub Deployment Guide

## 📋 الخطوات المطلوبة

### 1️⃣ إنشاء Repository على GitHub

1. افتح [GitHub](https://github.com) وسجل دخول
2. اضغط على زر **"New"** أو **"+"** → **"New repository"**
3. املأ البيانات:
   - **Repository name**: `quran-video-generator` (أو أي اسم تريده)
   - **Description**: `مُولِّد فيديوهات آيات القرآن الكريم - Quran Verses Video Generator`
   - اختر **Public** أو **Private** حسب رغبتك
   - **لا تختار** "Initialize with README" (لأن عندنا README بالفعل)
4. اضغط **"Create repository"**

### 2️⃣ ربط المشروع المحلي بـ GitHub

افتح PowerShell في مجلد المشروع وشغل الأوامر التالية:

```powershell
# التأكد من وجود git
git --version

# إذا لم يكن git مثبت، حمله من: https://git-scm.com/download/win

# الانتقال لمجلد المشروع
cd "c:\Users\Ahmed\Videos\تولي001\توليد1"

# التحقق من حالة Git
git status

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "Initial commit: Quran Video Generator with main_final.py"

# ربط المشروع بـ GitHub (استبدل USERNAME باسم المستخدم الخاص بك)
git remote add origin https://github.com/USERNAME/quran-video-generator.git

# رفع الملفات على GitHub
git push -u origin main

# إذا كان الفرع الرئيسي اسمه master بدلاً من main، استخدم:
# git push -u origin master
```

### 3️⃣ إذا واجهت مشكلة في الفرع الرئيسي

إذا كان اسم الفرع `master` وتريد تغييره لـ `main`:

```powershell
git branch -M main
git push -u origin main
```

### 4️⃣ التحقق من الرفع

بعد الرفع، افتح صفحة الـ repository على GitHub وتأكد من ظهور جميع الملفات.

---

## 🚀 تشغيل المشروع من GitHub

### للمستخدمين الجدد:

```bash
# 1. استنساخ المشروع
git clone https://github.com/USERNAME/quran-video-generator.git
cd quran-video-generator

# 2. إنشاء بيئة افتراضية (موصى به)
python -m venv .venv
.venv\Scripts\activate  # على Windows
# source .venv/bin/activate  # على Linux/Mac

# 3. تثبيت المتطلبات
pip install -r requirements.txt

# 4. تشغيل التطبيق
python main_final.py
```

التطبيق سيعمل على: `http://localhost:5000`

---

## 📦 الملفات المهمة

- ✅ `main_final.py` - الملف الرئيسي لتشغيل التطبيق
- ✅ `requirements.txt` - المكتبات المطلوبة
- ✅ `config.py` - الإعدادات
- ✅ `README.md` - الدليل الشامل
- ✅ `.gitignore` - الملفات المستبعدة من Git

---

## 🔒 ملاحظات أمنية

⚠️ **مهم جداً:**

1. **لا ترفع** ملفات تحتوي على:
   - مفاتيح API خاصة
   - كلمات مرور
   - بيانات شخصية

2. تأكد من وجود `.gitignore` يستبعد:
   - `.env` (ملفات البيئة)
   - `__pycache__/`
   - `.venv/` أو `venv/`
   - `output/` (الفيديوهات المُنتجة)
   - `temp/` (الملفات المؤقتة)
   - `backgrounds/` (الكاش)

---

## 🔄 تحديث المشروع على GitHub

عند إجراء تعديلات جديدة:

```powershell
# إضافة التغييرات
git add .

# عمل commit مع وصف التغيير
git commit -m "وصف التحديث"

# رفع التحديثات
git push
```

---

## 🌐 نشر المشروع على GitHub Pages (اختياري)

⚠️ **ملاحظة:** هذا المشروع يحتاج خادم Flask، لذا لا يمكن نشره مباشرة على GitHub Pages.

للنشر، ستحتاج إلى:
- **Heroku** (مجاني للمشاريع الصغيرة)
- **Railway** (بديل حديث)
- **Render** (خيار جيد)
- **PythonAnywhere** (مناسب للمشاريع Python)

---

## ❓ استكشاف الأخطاء

### مشكلة: `git: command not found`
**الحل:** ثبت Git من https://git-scm.com/download/win

### مشكلة: `Permission denied (publickey)`
**الحل:** أضف SSH key أو استخدم HTTPS مع Personal Access Token

### مشكلة: `failed to push some refs`
**الحل:** 
```powershell
git pull origin main --rebase
git push origin main
```

---

## 📞 الدعم

إذا واجهت أي مشكلة، يمكنك:
1. فتح Issue على GitHub
2. مراجعة [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
3. التواصل مع المطور

---

**بالتوفيق! 🚀**
