# 📋 دليل تحديث المشروع - Quran Reel Generator

## 🎯 الهدف من هذا الدليل
هذا الدليل يشرح كيفية تحديث المشروع عند استلام ملفات محدثة من المطور أو من مصدر آخر.

---

## 📦 ما هي الملفات التي يجب تحديثها؟

### 1. **ملفات Python الأساسية** (`.py`)
هذه الملفات تحتوي على الكود البرمجي الرئيسي:
- `config.py` - إعدادات المشروع
- `pexels_api.py` - التعامل مع API فيديوهات Pexels
- `quran_api.py` - جلب آيات القرآن
- `video_generator.py` - توليد الفيديوهات
- `enhanced_generator.py` - المولد المحسن
- `filter_pexels_videos.py` - فلترة الفيديوهات
- `review_videos.py` - مراجعة الفيديوهات
- `main_final.py` - الملف الرئيسي للتشغيل

### 2. **ملف المتطلبات** (`requirements.txt`)
يحتوي على قائمة المكتبات المطلوبة للمشروع.

### 3. **ملفات التوثيق** (`.md`)
- `README.md` - الدليل الرئيسي
- `QUICK_START.md` - دليل البدء السريع
- `TROUBLESHOOTING.md` - حل المشاكل
- وغيرها من ملفات التوثيق

### 4. **ملفات الإعدادات**
- `.gitignore` - ملفات يتم تجاهلها في Git

---

## 🔄 خطوات تحديث المشروع

### الطريقة الأولى: التحديث اليدوي (موصى بها للمبتدئين)

#### الخطوة 1: عمل نسخة احتياطية
```powershell
# انتقل إلى مجلد المشروع (توليد1)
cd .\توليد1

# إنشاء مجلد للنسخ الاحتياطية مع التاريخ في المجلد السابق
$backupFolder = "backup_$(Get-Date -Format 'yyyy-MM-dd_HH-mm-ss')"
New-Item -ItemType Directory -Path "..\$backupFolder"

# نسخ كل الملفات
Copy-Item -Path ".\*" -Destination "..\$backupFolder" -Recurse -Exclude ".venv","__pycache__","output","temp","test_output"
```

#### الخطوة 2: استبدال الملفات المحدثة
1. افتح مجلد المشروع `توليد1`
2. احذف الملف القديم الذي تريد تحديثه
3. انسخ الملف الجديد إلى نفس المكان
4. تأكد من أن اسم الملف هو نفسه تماماً

#### الخطوة 3: تحديث المكتبات (إذا تم تحديث requirements.txt)
```powershell
# تأكد أنك داخل مجلد توليد1
# تفعيل البيئة الافتراضية
.\.venv\Scripts\Activate.ps1

# تحديث المكتبات
pip install -r requirements.txt --upgrade
```

#### الخطوة 4: اختبار المشروع
```powershell
# تشغيل المشروع للتأكد من عمله
python main_final.py
```

---

### الطريقة الثانية: استخدام Git (للمستخدمين المتقدمين)

إذا كان المشروع على GitHub أو Git:

```powershell
# الانتقال لمجلد المشروع
cd .\توليد1

# جلب آخر التحديثات
git pull origin main

# تحديث المكتبات
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt --upgrade
```

---

## 📝 قائمة التحقق بعد التحديث

- [ ] ✅ تم عمل نسخة احتياطية من المشروع القديم
- [ ] ✅ تم استبدال الملفات المحدثة
- [ ] ✅ تم تحديث المكتبات (إذا لزم الأمر)
- [ ] ✅ تم اختبار المشروع والتأكد من عمله
- [ ] ✅ تم التحقق من ملفات الإعدادات (API Keys, etc.)

---

## 🔑 ملاحظات مهمة

### 1. **ملف الإعدادات (config.py)**
⚠️ **تحذير**: عند تحديث `config.py`، تأكد من الحفاظ على:
- مفتاح Pexels API الخاص بك
- أي إعدادات شخصية قمت بتغييرها

**الحل**: قبل الاستبدال، افتح الملف القديم وانسخ:
```python
PEXELS_API_KEY = "your_api_key_here"  # احفظ هذا
```
ثم الصقه في الملف الجديد بعد الاستبدال.

### 2. **البيئة الافتراضية (.venv)**
❌ **لا تحذف أو تستبدل مجلد `.venv`**
- هذا المجلد يحتوي على المكتبات المثبتة
- فقط قم بتحديث المكتبات باستخدام `pip install -r requirements.txt --upgrade`

### 3. **المجلدات التي لا يجب حذفها**
- `backgrounds/` - الفيديوهات الخلفية المحملة
- `output/` - الفيديوهات المولدة
- `fonts/` - الخطوط العربية
- `temp/` - الملفات المؤقتة

---

## 🆘 حل المشاكل الشائعة

### المشكلة 1: الملف الجديد لا يعمل
**الحل**:
1. تأكد من أن اسم الملف صحيح تماماً
2. تحقق من أنك في البيئة الافتراضية
3. راجع ملف `TROUBLESHOOTING.md`

### المشكلة 2: خطأ في المكتبات
**الحل**:
```powershell
# إعادة تثبيت المكتبات
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt --force-reinstall
```

### المشكلة 3: فقدان الإعدادات
**الحل**:
1. ارجع للنسخة الاحتياطية
2. انسخ ملف `config.py` القديم
3. قارنه مع الملف الجديد واحتفظ بإعداداتك

---

## 📊 جدول الملفات وأولوية التحديث

| الملف | الأولوية | يحتاج نسخ احتياطي؟ | ملاحظات |
|-------|----------|-------------------|---------|
| `config.py` | 🔴 عالية | ✅ نعم | احفظ API Keys |
| `requirements.txt` | 🔴 عالية | ⚠️ اختياري | قد تحتاج تحديث المكتبات |
| `*.py` (ملفات Python) | 🟡 متوسطة | ✅ نعم | استبدل مباشرة |
| `*.md` (ملفات التوثيق) | 🟢 منخفضة | ❌ لا | استبدل مباشرة |
| `.venv/` | ⛔ لا تحدث | - | فقط حدث المكتبات |
| `backgrounds/` | ⛔ لا تحدث | - | احتفظ بها |
| `output/` | ⛔ لا تحدث | - | احتفظ بها |

---

## 🚀 سكريبت تحديث سريع

احفظ هذا السكريبت في ملف `update.ps1` لتحديث سريع:

```powershell
# سكريبت التحديث السريع
# الاستخدام: .\update.ps1

Write-Host "🔄 بدء عملية التحديث..." -ForegroundColor Cyan

# 1. عمل نسخة احتياطية
$backupFolder = "backup_$(Get-Date -Format 'yyyy-MM-dd_HH-mm-ss')"
Write-Host "📦 إنشاء نسخة احتياطية في: $backupFolder" -ForegroundColor Yellow
New-Item -ItemType Directory -Path "..\$backupFolder" -Force | Out-Null
Copy-Item -Path ".\*" -Destination "..\$backupFolder" -Recurse -Exclude ".venv","__pycache__","output","temp","test_output","backgrounds"

# 2. تفعيل البيئة الافتراضية
Write-Host "🔧 تفعيل البيئة الافتراضية..." -ForegroundColor Yellow
& .\.venv\Scripts\Activate.ps1

# 3. تحديث المكتبات
Write-Host "📚 تحديث المكتبات..." -ForegroundColor Yellow
pip install -r requirements.txt --upgrade --quiet

# 4. اختبار
Write-Host "✅ التحديث اكتمل! يمكنك الآن استبدال الملفات المحدثة." -ForegroundColor Green
Write-Host "⚠️  تذكر: احتفظ بإعدادات config.py الخاصة بك!" -ForegroundColor Red
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع ملف `TROUBLESHOOTING.md`
2. تحقق من النسخة الاحتياطية
3. اتصل بالمطور

---

## 📅 سجل التحديثات

احتفظ بسجل للتحديثات هنا:

| التاريخ | الملفات المحدثة | الملاحظات |
|--------|-----------------|-----------|
| 2026-02-02 | - | المشروع الأولي |
|  |  |  |
|  |  |  |

---

**آخر تحديث**: 2026-02-02  
**الإصدار**: 1.0
